

export function isStringValid(s: string){
    return s !== undefined && s !== null && s.length !== 0;
}

export function isArrayValid(s: any[]){
    return s !== undefined && s !== null && s.length !== 0;
}